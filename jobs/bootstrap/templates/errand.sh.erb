#!/bin/bash

set -eux # report the usage of uninitialized variables

echo "Started bootstrap errand ..."

JOB_DIR=/var/vcap/jobs/bootstrap
CONFIG_PATH=$JOB_DIR/config/config.yml

/var/vcap/packages/bootstrap/bin/cf-mysql-bootstrap -configPath=${CONFIG_PATH}
echo "Bootstrap errand completed"

exit 0
