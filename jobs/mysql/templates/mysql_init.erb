UPDATE mysql.user SET Password=PASSWORD('<%= properties.mysql.root_password %>') WHERE User='root';
FLUSH PRIVILEGES;

SET GLOBAL fast_index_creation=false;

-- For now, provide credentials from this db
-- DROP DATABASE IF EXISTS test;
