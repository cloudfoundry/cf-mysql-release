GRANT ALL PRIVILEGES ON *.* TO '<%= properties.mysql.admin_username %>'@'%' WITH GRANT OPTION;
UPDATE mysql.user SET Password=PASSWORD('<%= properties.mysql.admin_password %>') WHERE User='<%= properties.mysql.admin_username %>';
DELETE FROM mysql.user WHERE User='';

FLUSH PRIVILEGES;

SET GLOBAL fast_index_creation=false;

-- For now, provide credentials from this db
-- DROP DATABASE IF EXISTS test;
