#!/bin/bash

set -e

if [ -z "$1" ]
  then echo "No directory supplied"
  exit 1
fi

echo "Deleting GRA logs older than 60 days from: $1 "
pattern=$1/GRA*.log
find $pattern -mtime +60 2>/dev/null | xargs rm -f
